apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
  - protocol: TCP
    port: 80 # 외부에서 80 포트로 접속
    targetPort: 80 # 컨테이너의 80 포트로 전달
