apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
- ../../base

namespace: dev

# 이미지 태그는 나중에 Jenkins 파이프라인에서 동적으로 변경합니다.
images:
- name: kyla333/command-service
  newTag: dev-latest
- name: kyla333/query-service
  newTag: dev-latest
- name: kyla333/todo-frontend
  newTag: dev-latest

patchesStrategicMerge:
- patch-replicas.yaml
